# GEFS 全变量矫正冻结与 checkpoint 分支状态

更新时间：2026-07-21

## 已确认结论

- 降水采用按站点的七日累计两阶段线性缩放，因子收缩系数为 `0.75`。
- 2015-2019 历史应用必须使用逐目标年的因果扩展拟合因子，并满足 `fit_last_year < target_year`。使用 2000-2019 全量拟合的产物只允许应用于 2024。
- 非降水变量以对应年份 ERA5 为参考完成无泄漏选择，冻结参数如下：
  - 温度中心：`alpha=1.0`
  - 昼夜温差：`alpha=0.0`
  - 实际水汽压：`alpha=0.75`
  - 风速：`alpha=1.0`
  - 太阳辐射仿射矫正：`alpha=0.25`
- 2019 ERA5 在气象矫正阶段是验证参考，不是 SWAP 标签。代理模型标签必须由矫正 GEFS 驱动 SWAP 后生成。

## 服务器审计状态

非降水正式五成员多周期验证已经通过，随后完成了降水与非降水冻结策略合并。统一天气审计状态为：

`frozen_all_variable_weather_integration_passed`

审计覆盖 5 个周期、5 个站点、5 个成员和未来 7 天，共 875 行。以下计数均为 0：

- 因子拟合年份泄漏；
- 缺失值和非有限值；
- 正值物理变量出现负值；
- 最低温高于最高温；
- 降水零值位置改变；
- 降水成员排序反转。

七日降水累计最大绝对误差为 `7.105427357601002e-15 mm`，属于浮点误差。

## 季节主干与 checkpoint

P1/2015 已完成 ERA5 连续季节主干模拟，并按每 7 天生成 14 个决策 checkpoint。所有 checkpoint 与完整季节模拟在对应日期的作物状态和土壤剖面状态完全一致，最大绝对误差均为 0。

下一道验证固定为：

`P1 / 2015-07-06 / 8 irrigation candidates`

该 smoke 必须直接读取 `2015-07-05` 的已验证 checkpoint，不重新运行决策日前主干。未来七天使用冻结矫正 GEFS 五成员逐日集合均值，并枚举：

`[0, 10, 15, 20, 25, 30, 40, 60] mm`

runner 和本地测试已经完成，但服务器 SWAP smoke 尚未执行，因此不能把该阶段记录为通过。

## 当前边界

- 尚未生成完整 2015-2019 密集日期标签。
- 尚未开始代理模型训练。
- 尚未执行 2024 测试或 TTA。
- 单站点 checkpoint smoke 通过后，才扩展到五站点。
